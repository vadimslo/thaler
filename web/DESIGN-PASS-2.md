# Design pass 2: from editorial page to live terminal

References the owner named as the bar (do not copy, match the density and energy):
- standardreserve.xyz: numbered sections ("01 / THE MECHANISM"), uppercase micro-labels, hero with two doors, flow diagram with arrows, "OPEN FOR INSPECTION: see the system as it stands", live SUPPLY / EPOCH readouts in the hero.
- inferra.trade demo (frames in /private/tmp/claude-501/-Users-vadimsl/b4c69239-e832-4bc9-80f2-c1b8c9a25fa3/scratchpad/inferra_*.jpg): a global status strip under the header (UTC clock, network, "ENGINE LIVE", counters), stat tiles with big monospace numbers and small deltas, dense cards with label/value rows, filled accent CTA buttons, a revenue-split block with horizontal bars and per-line amounts, a live fill/activity table.

Keep: palette (near-black, off-white, brass), Fraunces / Inter / JetBrains Mono, hairlines, no gradients, no emoji, mobile at 375px.

Change:
1. Status strip (all pages, under the header, 32px, mono uppercase 11px): `SEPOLIA · BLOCK 9,123,456 · EPOCH 12 · 03:12:44 TO NEXT · MULT 1.00× · EXPANSION · BUY TAX 41.2% · SELL TAX 42.6% · ● LIVE`. Values from the same hooks; clock ticks every second client-side; dot pulses.
2. Home hero: headline left, three stat tiles right (Circulating supply, Burned, Treasury ETH) plus a compact "Epoch 12 · 03:12 to next" line. Filled brass primary button "Acquire a charter", ghost secondary "Buy THALER".
3. Mechanism as a real SVG flow diagram (inline, theme colors): ETH buys / ETH sells → canonical pool (FlowHook) → flow signal → central bank → EXPANSION VAULT (reserves) / CONTRACTION VAULT (buyback + burn) / POL / TEAM, with small animated dashes along the arrows (CSS stroke-dashoffset). Numbered sections: 01 / THE MECHANISM, 02 / FIND YOUR ENTRY, 03 / THE SYSTEM AS IT STANDS (live dashboard preview: supply bar segmented genesis POL / issued / burned / remaining budget; treasury split bars like the Inferra revenue block with ETH amounts; last 8 events), 04 / OPEN FOR INSPECTION (contracts, whitepaper).
4. Activity feed component (home + protocol): reads events from the deployment block in 5,000-block windows via viem getLogs (publicnode caps ranges), newest first, 20 rows: time, type (Taxed buy/sell, FoundingMinted, AuctionBought, BranchOpened, Withdrawn, CharterResolved, EpochRolled, Buyback, PolCompounded, Allocated), amount, tx link. Cache last scanned block in localStorage.
5. Charters: founding card with a progress bar (minted / 1000) and a large price; auction card with the current price as a big number and a small SVG curve of today's decay (start → floor over 24h, marker at now); "My charters" as cards: id, ten branch pips (filled = open), pending THALER counting up every second (interpolate from issuancePerSecond / totalBranches × branches between polls), fee right now (from resolveFeeBps, label it "withdrawal fee now"), buttons Withdraw (primary), Open branch (shows license price and approve step), Resolve (danger, confirm).
6. Token: swap card is the hero of the page (right column, sticky): tabs Buy / Sell, amount input with MAX, live quote, a line "Tax right now: 41.2% (falls to 2.0%)", approve step for sell, big filled button. Left: supply bar (segmented) with legend and numbers; "what burns supply" as three rows with live counters (licenses burned, buybacks burned, resolution fees unminted if exposed, else omit).
7. Protocol: a dashboard grid of stat tiles (multiplier, regime, epoch and countdown, issuance/branch/day, total branches, issued of 900M with bar, license price + left today, charter auction price + left today, unallocated, expansion, contraction, POL vault, team vault, POL liquidity, next buyback, last tick), then the treasury split bars, then two action buttons "Roll epochs" / "Tick treasury" with tx states, then the full parameter table (testnet column from the chain where readable, else from config).
8. Contracts: table with address, Etherscan link, "verified" badge (static), pool id, plus a short "how to verify yourself" note (forge verify command).
9. Micro-interactions: numbers use a tiny count-up on change; cards fade in on mount; hover on cards lifts the hairline to brass. No parallax, no heavy motion.
10. All new copy in the same register as the whitepaper. No exclamation marks, no emoji, no em-dashes.

Definition of done: `npm run build` passes; every page at 1440 and 375 without horizontal scroll; zero console errors with live addresses; screenshots of home, token, charters, protocol at both widths saved to web/screenshots/.
